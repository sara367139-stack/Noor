import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // ==========================
  // Brand Colors
  // ==========================

  /// Main Brand Color
  // static const Color primary = Color(0xFF10B981);

  /// Secondary Dark Color
  static const Color secondary = Color.fromARGB(255, 20, 57, 91);

  /// Light Background
  static const Color tertiary = Color(0xFFF8FAFC);

  /// Neutral Gray
  static const Color neutral = Color(0xFF64748B);

  // ==========================
  // Background
  // ==========================

  // static const Color background = tertiary;
  static const Color darkBackground = Color.fromARGB(255, 19, 62, 102);

  // ==========================
  // Text
  // ==========================

  static const Color textPrimary = Color.fromARGB(255, 219, 221, 226);
  static const Color textSecondary = Color.fromARGB(255, 128, 142, 161);

  static const Color textWhite = Colors.white;

  // ==========================
  // Basic Colors
  // ==========================

  // static const Color white = Colors.white;
  static const Color black = Color.fromARGB(255, 16, 49, 75);
 // static const Color transparent = Color.fromARGB(0, 210, 185, 185);

  // ==========================
  // Status Colors
  // ==========================

  // static const Color success = Color(0xFF22C55E);
  // static const Color warning = Color(0xFFF59E0B);
  // static const Color error = Color(0xFFEF4444);
  // static const Color info = Color(0xFF3B82F6);

  // ==========================
  // Borders & Divider
  // ==========================

  static const Color border = Color(0xFFE2E8F0);
  // static const Color divider = Color(0xFFE5E7EB);

  // ==========================
  // Card
  // ==========================

  static const Color cardLight = Colors.white;
  static const Color cardDark = Color.fromARGB(255, 16, 49, 75);

  // ==========================
  // Disabled
  // ==========================

  static const Color disabled = Color(0xFFCBD5E1);
  // static const Color white70 = Colors.white70;




//   static const Color background = Color(0xFF0F172A);

// static const Color surface = Color(0xFF1E293B);

static const Color card = Color(0xFF243447);

static const Color activeCard = Color(0xFF0F766E);

static const Color activeBorder = Color(0xFF10B981);

static const Color primary = Color(0xFF10B981);

static const Color white = Colors.white;

static const Color white70 = Colors.white70;

static const Color greyText = Color(0xFF94A3B8);

static const Color divider = Color(0xFF334155);

  static Color? get background => null;
}